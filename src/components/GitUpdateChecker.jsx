import React from 'react';

const GitUpdateChecker = ({ customerId }) => (
    <div className="text-xs p-2 bg-gray-100 rounded mt-4">
      GitUpdateChecker for customer {customerId}
    </div>
);

export default GitUpdateChecker;
